# Core backend
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
requests>=2.28.0
psutil>=5.9.0
pydantic>=2.0.0

# Audio ducking (Windows only) – required for "Enable Audio Ducking"
pycaw>=20230407
comtypes>=1.2.0

# Optional: GPU usage in System tab (NVIDIA only; chart shows "—" if not installed or no NVIDIA GPU)
pynvml>=11.0.0

# Optional: voice commands, Spotify, tools
# speech_recognition
# spotipy
# pyautogui
# python-dotenv
